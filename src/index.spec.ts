// import { getAge } from '.';

describe('getAge', () => {
  it('should work', () => {
    // todo
    // const now = new Date();
    // const birth = new Date(
    //   now.getFullYear() -3,
    //   now.getMonth(),
    //   now.getUTCDate()
    // );
    // console.log(getAge(now, { now: birth, levels: 5, }));
  });
});